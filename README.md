# Dự án Trang CV Cá Nhân (Curriculum Vitae)

Đây là dự án xây dựng một trang CV cá nhân hoàn chỉnh bằng HTML và CSS. Dự án được tạo ra với mục đích luyện tập và áp dụng các kiến thức front-end cốt lõi.

## 🌟 Đặc điểm nổi bật

Dự án này tập trung vào 3 kỹ thuật CSS quan trọng:

1.  **Box Model:** Áp dụng `padding`, `margin`, `border` và `box-shadow` để tạo khối và giao diện sạch sẽ cho CV.
2.  **Flexbox:** Toàn bộ bố cục chính của trang (sidebar trái và nội dung chính bên phải) được xây dựng bằng `display: flex`, giúp căn chỉnh bố cục dễ dàng.
3.  **Responsive Design:** Trang web có khả năng "biến hình" (responsive) bằng cách sử dụng `@media queries`. Khi xem trên thiết bị di động (dưới 768px), bố cục 2 cột sẽ tự động chuyển thành 1 cột duy nhất, xếp chồng lên nhau để dễ đọc hơn.
4.  **Hiệu ứng CSS:** Sử dụng `transition` và `:hover` để tạo hiệu ứng chuyển động mượt mà cho các mục kỹ năng và dự án.
5.  **CSS Grid:** Sử dụng `display: grid` để chia cột cho danh sách các dự án.

## 🚀 Công nghệ sử dụng

* **HTML5:** Cung cấp "khung xương" và cấu trúc ngữ nghĩa cho trang web.
* **CSS3:** "Mặc áo" và tạo kiểu dáng cho trang, bao gồm Flexbox, Grid, Media Queries và các hiệu ứng.
* **Font Awesome:** Sử dụng cho các icon (biểu tượng) trong phần thông tin liên hệ.
* **Google Fonts:** Sử dụng font 'Roboto' để đảm bảo tính nhất quán về chữ viết.

## 📺 Xem trực tiếp (Live Demo)
Bạn có thể xem trang web này hoạt động tại đây:`[akalong321.github.io/Curriculum-Vitae/]`
